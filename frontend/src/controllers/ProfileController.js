import React from 'react';
import Profile from '../views/Profile';

const ProfileController = () => {
  // Ajoutez ici la logique nécessaire pour gérer le profil de l'utilisateur, par exemple, en utilisant le modèle UserModel.

  return (
    <Profile />
  );
};

export default ProfileController;