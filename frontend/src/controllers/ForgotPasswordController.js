import React from 'react';
import ForgotPassword from '../views/ForgotPassword';

const ForgotPasswordController = () => {
  // Ajoutez ici la logique nécessaire pour gérer la récupération du mot de passe oublié, par exemple, en utilisant le modèle UserModel.

  return (
    <ForgotPassword />
  );
};

export default ForgotPasswordController;